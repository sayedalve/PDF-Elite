*** Begin Patch
*** Update File: .taskfiles/backend.yml
@@
-  build:ci:
-    desc: "Build for CI (formatting checked separately)"
-    cmds:
-      - cmd: cmd /c ".\gradlew.bat build -PnoSpotless"
-        platforms: [windows]
-      - cmd: ./gradlew build -PnoSpotless
-        platforms: [linux, darwin]
+  build:ci:
+    desc: "Build for CI (formatting checked separately)"
+    cmds:
+      # For Windows CI runs: add --stacktrace --info --console=plain to ensure failing-test details are printed to the job log
+      - cmd: cmd /c ".\gradlew.bat build -PnoSpotless --stacktrace --info --console=plain"
+        platforms: [windows]
+      - cmd: ./gradlew build -PnoSpotless
+        platforms: [linux, darwin]
@@
-  test:
-    desc: "Run backend tests"
-    cmds:
-      - cmd: cmd /c ".\gradlew.bat test"
-        platforms: [windows]
-      - cmd: ./gradlew test
-        platforms: [linux, darwin]
+  test:
+    desc: "Run backend tests"
+    cmds:
+      # Diagnose tests with full stacktrace/info output in Windows CI to help identify failing test
+      - cmd: cmd /c ".\gradlew.bat test --stacktrace --info --console=plain"
+        platforms: [windows]
+      - cmd: ./gradlew test
+        platforms: [linux, darwin]
*** End Patch