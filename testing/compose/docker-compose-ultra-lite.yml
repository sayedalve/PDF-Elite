services:
  pdf-elite:
    build:
      context: ../..
      dockerfile: docker/embedded/Dockerfile.ultra-lite
    container_name: pdf-elite-Ultra-Lite
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 2G
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080$${SYSTEM_ROOTURIPATH:-''}/api/v1/info/status | grep -q 'UP'"]
      interval: 5s
      timeout: 10s
      retries: 16
    ports:
      - "8080:8080"
    volumes:
      - ../../PDFElite/latest/config:/configs:rw
      - ../../PDFElite/latest/logs:/logs:rw
    environment:
      DISABLE_ADDITIONAL_FEATURES: "true"
      SECURITY_ENABLELOGIN: "false"
      ENDPOINTS_GROUPS_TO_REMOVE: "CLI"
      SYSTEM_DEFAULTLOCALE: en-US
      UI_APPNAME: pdf-elite-Ultra-lite
      UI_HOMEDESCRIPTION: Demo site for pdf-elite-Ultra-lite Latest
      UI_APPNAMENAVBAR: pdf-elite-Ultra-lite Latest
      SYSTEM_MAXFILESIZE: "100"
      METRICS_ENABLED: "true"
      SYSTEM_GOOGLEVISIBILITY: "true"
      SHOW_SURVEY: "true"
    networks:
      - PDFElite-network

networks:
  PDFElite-network:
    driver: bridge

volumes:
  PDFElite-data:
  PDFElite-config:
  PDFElite-logs:
